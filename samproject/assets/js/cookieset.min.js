function setCookies() {
    var a = window.location.search;
    document.cookie = "adcookie=true", veri = a.split("&"), veri.forEach(function (a) {
        a = a.split("="), setCookie("adtrack_" + a[0].replace("?", ""), a[1])
    })
}
function getUrlParameter(a) {
    a = a.replace(/[\[]/, "\\[").replace(/[\]]/, "\\]");
    var b = new RegExp("[\\?&]" + a + "=([^&#]*)"), c = b.exec(location.search);
    return null === c ? "" : decodeURIComponent(c[1].replace(/\+/g, " "))
}
function setCookie(a, b) {
    var c = new Date;
    c.setTime(c.getTime() + 2592e6);
    var d = "expires=" + c.toUTCString();
    document.cookie = a + "=" + b + ";" + d + ";path=/"
}

function set5Cookie(cname, cvalue) {
    var d = new Date();
    d.setTime(d.getTime() + (5 * 60 * 60 * 1000));
    var expires = "expires=" + d.toUTCString();
    document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
}

function setYearCookie(cname, cvalue) {
    var d = new Date();
    d.setTime(d.getTime() + 31536000000);
    var expires = "expires=" + d.toUTCString();
    document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
}


if (!getCookie('adtrack_lasthost')) {
    set5Cookie('adtrack_lasthost', document.location.href);
}


if (!getCookie('adtrack_firsthost')) {
    setYearCookie('adtrack_firsthost', document.location.href);
}

if (!getCookie('adtrack_lastreferrer')) {
    set5Cookie('adtrack_lastreferrer', document.referrer);
}

if (!getCookie('adtrack_firstreferrer')) {
    setYearCookie('adtrack_firstreferrer', document.referrer);
}


function getCookie(a) {
    for (var b = a + "=", c = decodeURIComponent(document.cookie), d = c.split(";"), e = 0; e < d.length; e++) {
        for (var f = d[e]; " " == f.charAt(0);)f = f.substring(1);
        if (0 == f.indexOf(b))return f.substring(b.length, f.length)
    }
    return !1
}
function adtrack() {
    getCookie("adcookie") ? getCookie("adtrack_utm_source") != getUrlParameter("utm_source") && setCookies() : setCookies()
}
adtrack();